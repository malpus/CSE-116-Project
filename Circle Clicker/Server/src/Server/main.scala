package Server

import play.api

object main {
  val maxX: Int = 1000 //Max X value of Playing Field
  val maxY: Int = 1000 //Max Y value of Playing Field
  val defaultRadius: Int = 5 //Default Starting Radius of Player Circle
  val sizeChange: Int = 1 //Radius change on click
}
