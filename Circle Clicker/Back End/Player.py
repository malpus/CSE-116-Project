class Circle(name, posX, posY, amount):
    def updateLocation(x,y):
        posx = x
        posy = y
    def checkElimination(time, a):
        if (time < 0 and self.amount > 10):
            self.amount = 0
